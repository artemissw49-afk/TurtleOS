-- ============================================================
-- config.lua  —  Конфигурация системы автокрафта
-- Отредактируйте имена периферий под свою сеть.
-- ============================================================

return {
    -- ── Периферии ─────────────────────────────────────────────
    monitor = "monitor_2",             -- Имя монитора в сети
    fluids_monitor = "monitor_1",      -- Маленький монитор управления жидкостями
    barrel  = "minecraft:barrel_1",    -- Бочка для записи рецептов
    turtle  = "turtle_8",              -- Черепашка-крафтер
    turtle_rednet_id = 3,              -- ID rednet для крафтовой черепашки (null = broadcast)

    -- ── Каналы связи с черепашкой (modem) ────────────────────
    turtle_cmd_ch   = 100,   -- Компьютер -> черепашка (не используется при rednet)
    turtle_reply_ch = 101,   -- Черепашка -> компьютер (не используется при rednet)
    turtle_timeout  = 15,    -- Секунд ожидания ответа

    -- ── Тип хранилища (авто-поиск по сети) ───────────────────
    vault_type = "create:item_vault",

    -- ── Танки жидкостей ───────────────────────────────────────
    -- Эти 5 танков управляются с маленького монитора (кнопка <<).
    -- Их содержимое НЕ отображается во вкладке FLUIDS.
    excluded_super_tanks = {
        "gtceu:lv_super_tank_44",
        "gtceu:lv_super_tank_45",
        "gtceu:lv_super_tank_46",
        "gtceu:lv_super_tank_47",
        "gtceu:lv_super_tank_48",
    },

    -- ── Файлы данных ──────────────────────────────────────────
    recipes_file = "/data/recipes.json",

    -- ── Тайминги ─────────────────────────────────────────────
    craft_timeout  = 300,  -- Макс. ожидание завершения в машине (сек)
    check_interval = 0.5,    -- Интервал проверки машины (сек)
    stock_refresh  = 5,    -- Интервал обновления инвентаря (сек)

    -- ── Масштаб текста на мониторе ────────────────────────────
    -- 0.5 = мелкий (больше текста), 1 = нормальный, 2 = крупный
    text_scale = 1,

    -- ── Фильтрация машин в списке "+ RECIPE" ─────────────────
    -- Периферии, которые никогда не показываются в списке машин
    excluded_machines = {
        "gtceu:lv_super_tank", "left", "right", "top", "bottom", "back",
    },
    -- Машины, которые выводятся по отдельности (с номером / содержимым).
    -- Все остальные машины одного типа объединяются в одну строку.
    special_machines = { "extruder", "chemical_reactor" },

    -- ── Конвенция бочки при записи рецепта ───────────────────
    -- Слот 1 = результат крафта
    -- Слоты 4,5,6,13,14,15,22,23,24 = ингредиенты
    --     для turtle: позиционый 3x3-гр grid как на верстаке
    --     (соответствуют слотам черепашки 1,2,3,5,6,7,9,10,11)
    -- Слоты 2+   = ингредиенты (для machine: без учёта позиции)
}
